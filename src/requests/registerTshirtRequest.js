"use strict";
exports.__esModule = true;
var registerTshirtRequests = /** @class */ (function () {
    function registerTshirtRequests(color, size, label) {
        color !== void (0) && (this.color = color);
        size !== void (0) && (this.size = size);
        label !== void (0) && (this.label = label);
    }
    return registerTshirtRequests;
}());
exports.registerTshirtRequests = registerTshirtRequests;
